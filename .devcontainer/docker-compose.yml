version: "3.8"
services:
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/dev-ai-driven-development:cached
    command: sleep infinity
